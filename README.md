# openai_function_calling_concepts
Repository to work some functional calling concepts

# Install requirements
!pip install -r requirements.txt

# OpenAi API
Create a .env and add the line OPENAI_API_KEY = <your openai api key>